import React from 'react';

const Spinner = size => {
    return (
        <span
            class="spinner"
            style={{ width: size, height: size }}
        >
        </span>
    )
}

export default Spinner;